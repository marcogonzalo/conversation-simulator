# =============================================================================
# APPLICATION CONFIGURATION (Root .env file)
# =============================================================================
# This file contains application-level settings (API keys, models, audio)
# For environment settings (database, debug), see backend/config/*.env files
# Copy this file to .env and configure your values
# =============================================================================

# OpenAI API Configuration
OPENAI_API_KEY=sk-your-key-here

# OpenAI Voice-to-Voice Configuration
OPENAI_VOICE_MODEL=4o-mini-realtime-preview
OPENAI_VOICE_TEMPERATURE=0.8
OPENAI_VOICE_MAX_TOKENS=4096

# AI Provider Configuration
# TEXT_AI_PROVIDER: Provider for text-based conversations and analysis (openai, gemini, anthropic)
# VOICE_AI_PROVIDER: Provider for voice-to-voice conversations (openai, gemini)
# AI_PROVIDER: Legacy fallback (will be used if TEXT_AI_PROVIDER/VOICE_AI_PROVIDER not set)
TEXT_AI_PROVIDER=openai
VOICE_AI_PROVIDER=openai
AI_PROVIDER=openai

# Text AI Model Configuration
AI_MODEL=gpt-4o-mini
AI_TEMPERATURE=0.7
AI_MAX_TOKENS=1000

# Audio Settings
AUDIO_CHANNELS=1
AUDIO_LATENCY_TARGET=500
AUDIO_PLAYBACK_SAMPLE_RATE=24000
# Audio output format for AI responses: 'webm' (default, compressed, better for streaming) or 'wav' (uncompressed, larger)
AUDIO_OUTPUT_FORMAT=webm

# Voice Detection Settings
VOICE_DETECTION_THRESHOLD=0.5
VOICE_DETECTION_PREFIX_PADDING_MS=300
VOICE_DETECTION_SILENCE_DURATION_MS=500

# Prompt Validation Settings
# Set to 'true' to enable strict validation (blocks prompts with critical semantic errors)
# Set to 'false' for permissive mode (logs warnings but allows all configurations)
# Recommended: false for development, true for production
PROMPT_STRICT_VALIDATION=false
